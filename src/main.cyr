# nein — Cyrius port
# Ported from 9338 lines of Rust

# Stdlib auto-included via cyrius.toml

fn main() {
    alloc_init();
    println("nein ready");
    return 0;
}

var r = main();
syscall(SYS_EXIT, r);
